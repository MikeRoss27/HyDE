# Standalone security-first entry point.
# It intentionally does not source HyDE's dynamic MPRIS/fingerprint boilerplate.
$BACKGROUND_PATH = $HOME/.cache/hyde/sddm-bg.png

source = $HOME/.config/hypr/hyprlock/Personal.conf

