# Generated by Gestures 0.3.0  -->  https://gitlab.com/cunidev/gestures
# Manual editing might result in data loss!


# Invalid lines


# Unsupported lines

# Swipe threshold (0-100)
swipe_threshold 0

# Gestures
gesture pinch clockwise 4 volumecontrol.sh -o i 10
gesture pinch anticlockwise 4 volumecontrol.sh -o d 10
gesture swipe up 4 bash -c 'f=$(( $(hyprctl -j activewindow | jq .fullscreen) == 0 )); hyprctl dispatch "hl.dsp.window.fullscreen_state({internal=$f, client=$f})"'
gesture swipe down 4 hyprctl dispatch 'hl.dsp.exec_cmd("screenshot.sh p")'  # print all monitor outputs
gesture swipe right 4  hyprctl dispatch 'hl.dsp.exec_cmd("pkill rofi")' || cliphist.sh c  # open Pasteboard in screen center
gesture swipe left 4 hyprctl dispatch 'hl.dsp.exec_cmd("swaync-client -t -sw")'
gesture pinch out 4  brightnesscontrol.sh  i
gesture pinch in 4  brightnesscontrol.sh  d